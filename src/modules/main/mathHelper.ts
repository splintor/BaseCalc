module main {
    export class MathHelper {
        addNumbers(a: number, b: number): number{
            return a + b;
        }

        subtractNumbers(a: number, b: number): number {
            return a - b;
        }
    }
}